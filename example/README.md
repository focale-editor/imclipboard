# Imclipboard example

This application demonstrates the two core operations:

- copying a bundled one-pixel PNG to the system clipboard;
- reading and displaying the current clipboard image.

The operations are exposed as buttons so the same example works with browser and iOS user-gesture requirements.
